<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Receitas</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&display=swap" rel="stylesheet">
    <style>
      .body-bg {
        background: linear-gradient(to bottom right, #FCA8A8, #7C8DDA);
      }
    </style>
  </head>
  <body class="body-bg min-h-screen pb-6 px-2 md:px-0" style="font-family:'Lato',sans-serif;">
    <div class="mt-10 flex flex-col items-center justify-center">
      <div class="bg-white mt-10 p-8 rounded-lg shadow-lg max-w-screen-md">
        <h1 class="text-xl text-rose-400 font-semibold mb-2"><%= @category.name %></h1>
        <% @recipes.each do |recipe| %>
          <%= link_to recipe.title, recipe_path(recipe), class: " hover:underline" %>
          <hr>
        <% end %>
        <div class="mt-4">
          <%= link_to 'Voltar', categories_path, class: "font-semibold hover:text-gray-600 ml-auto" %>
        </div>
      </div>
    </div>
  </body>
</html>
